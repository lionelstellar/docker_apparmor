#!/bin/bash

# user data
NAME='test'
MOUNT_DIR=$(dirname $(dirname `which $0`))"/data"
echo "host mount: "$MOUNT_DIR


/usr/bin/docker run --name $NAME \
    -v $MOUNT_DIR:/data  \
    --security-opt apparmor=docker-test \
    -w /home  -itd ubuntu.test:latest /bin/bash
